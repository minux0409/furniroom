/**
 * ============================================================
 * APP CONFIG — 프론트엔드 변경 가능한 모든 설정값
 * ============================================================
 */
export const APP_CONFIG = {
  // ============================================================
  // 🌐 API 서버
  // ============================================================
  API_BASE_URL: __DEV__
    ? "http://10.0.2.2:3000/api/v1" // Android 에뮬레이터 → localhost
    : "https://api.furniroom.com/api/v1", // 프로덕션

  // ============================================================
  // 🔑 Google OAuth
  // ============================================================
  GOOGLE_WEB_CLIENT_ID: process.env.EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID ?? "",

  // ============================================================
  // 💰 슬롯 가격 (표시용)
  // ============================================================
  PAID_SLOT_UNIT: 5,
  PAID_HOUSE_SLOT_PRICE_USD: 1.99,
  PAID_FURNITURE_SLOT_PRICE_USD: 1.99,

  // ============================================================
  // 🆓 무료 플랜 제한 (표시용)
  // ============================================================
  FREE_HOUSE_LIMIT: 1,
  FREE_FURNITURE_LIMIT: 12,

  // ============================================================
  // 📁 업로드 제한 (표시용)
  // ============================================================
  MAX_IMAGE_SIZE_MB: 10,
  MAX_MODEL_SIZE_MB: 50,

  // ============================================================
  // 🎨 UI
  // ============================================================
  DEFAULT_LANGUAGE: "ko",
} as const;
