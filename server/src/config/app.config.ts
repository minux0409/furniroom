/**
 * ============================================================
 * APP CONFIG — 변경 가능한 모든 설정값을 이 파일에서 관리합니다.
 * ============================================================
 * 무료 플랜 개수, 가격, AWS 정보 등을 바꾸고 싶을 때
 * 이 파일만 수정하면 됩니다.
 */
export const APP_CONFIG = {
  // ============================================================
  // 🏠 무료 플랜 제한
  // ============================================================
  FREE_HOUSE_LIMIT: 1,        // 무료로 등록 가능한 집 개수
  FREE_FURNITURE_LIMIT: 12,   // 무료로 등록 가능한 가구 개수

  // ============================================================
  // 💳 유료 슬롯 구매 단위 & 가격
  // ============================================================
  PAID_SLOT_UNIT: 5,                   // 한 번에 구매하는 슬롯 단위
  PAID_HOUSE_SLOT_PRICE_USD: 1.99,     // 집 슬롯 5개 가격 (달러)
  PAID_FURNITURE_SLOT_PRICE_USD: 1.99, // 가구 슬롯 5개 가격 (달러)

  // ============================================================
  // 🔓 기능 잠금 (true = 유료 기능, false = 무료 제공)
  // ============================================================
  IS_3D_VIEW_PREMIUM: false,     // 3D 뷰 유료 여부
  IS_AR_RULER_PREMIUM: false,    // AR 자 유료 여부
  IS_COMMUNITY_PREMIUM: false,   // 커뮤니티 유료 여부

  // ============================================================
  // 🗑️ 회원 탈퇴 후 데이터 보관 기간
  // ============================================================
  ACCOUNT_DELETION_GRACE_DAYS: 30,

  // ============================================================
  // 📁 파일 업로드 제한
  // ============================================================
  MAX_IMAGE_SIZE_MB: 10,
  MAX_MODEL_SIZE_MB: 50,

  // ============================================================
  // 🌍 다국어 지원
  // ============================================================
  DEFAULT_LANGUAGE: 'en',
  SUPPORTED_LANGUAGES: ['en', 'ko'],

  // ============================================================
  // ☁️ AWS (실제 값은 .env에서 관리)
  // ============================================================
  AWS_REGION: process.env.AWS_REGION ?? 'ap-northeast-2',
  S3_BUCKET_NAME: process.env.S3_BUCKET_NAME ?? 'furniroom-assets',
  CLOUDFRONT_URL: process.env.CLOUDFRONT_URL ?? '',

  // ============================================================
  // 🖥 서버
  // ============================================================
  API_PREFIX: 'api/v1',
  PORT: parseInt(process.env.PORT ?? '3000', 10),
} as const;
